# Verification record

**Command**: `npm run verify`  
**Executed**: 2026-08-13  
**Result**: pass

## Gates

- Prettier check: pass.
- ESLint with zero warnings: pass.
- Strict TypeScript project build: pass.
- Vitest coverage: 61 files, 416 tests passed.
- Coverage: 87.14% statements, 82.36% branches, 89.06% functions,
  88.32% lines. Configured critical domain-module thresholds also passed.
- Vite production build: pass (162 modules transformed).
- Playwright production-preview matrix: 49 tests passed: 33 functional
  journeys across desktop Chromium keyboard, tablet WebKit touch, and mobile
  WebKit touch, plus 16 deterministic visual screenshot/geometry checks.

This successful automated run does not complete T110 because T110 is ordered
after T109, which is waiting on the external manual accessibility and usability
evidence required by T107 and T108. The command must be rerun after those manual
records and any resulting corrections are complete.

## T110 rerun — 2026-08-17

**Command**: `npm run verify`
**Executed**: 2026-08-17, after the 2026-08-16/17 remediation and the approved
visual-baseline replacement (T111)
**Result**: pass (exit code 0)

### Gates

- Prettier check: pass. `.claude/` was added to `.prettierignore`; those files are
  generated by `specify init` and are not project source.
- ESLint with zero warnings: pass.
- Strict TypeScript project build: pass.
- Vitest coverage: 63 files, **439 tests** passed. Statements 86.29%
  (threshold 85), branches 80.36% (threshold 80), functions 87.17%, lines 87.38%.
- Production build: pass.
- Playwright: **49 tests passed** across all four projects —
  `desktop-chromium-keyboard`, `tablet-webkit-touch`, `mobile-webkit-touch`, and
  `visual-chromium` (16 visual checks against the newly approved baselines).

### Changes required to reach a passing run

- Journeys 02, 03, and 07 pinned calendar dates from their authoring week and had
  begun failing purely because that week passed. They now derive dates at run time
  through `e2e/journeys/dates.ts`, removing the recurring breakage.
- Two genuine defects were fixed: the decorative completion mark intercepted clicks
  aimed at the checkbox, and an action menu stayed open beneath a dialog it had
  opened. Both are covered by the journeys and by `TaskExecution.test.tsx`.
- `visual-chromium` now runs with `contextOptions: { reducedMotion: 'reduce' }`
  because the orbital field animates continuously on a canvas; the component draws
  one deterministic frame under reduced motion.

### Non-applicable gates

None. Every gate in `npm run verify` executed and passed. Manual accessibility
(T107) and timed usability (T108) are outside this command and were removed from
the plan by product-owner decision; see `traceability.md` for what that leaves
unevidenced.
